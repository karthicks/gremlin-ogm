language: java
jdk: oraclejdk8
sudo: false
script: 
  mvn clean install
after_success:
  - bash <(curl -s https://codecov.io/bash)
after_failure:
  - cat gremlin-objects/target/surefire-reports/*.txt
cache:
  directories:
  - $HOME/.m2
